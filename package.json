{
  "name": "@holochain-open-dev/file-storage-dev",
  "private": true,
  "workspaces": [
    "ui",
    "tests"
  ],
  "scripts": {
    "start": "npm run build:happ && concurrently -k \"npm run start:happ\" \"npm run start -w @holochain-open-dev/file-storage\"",
    "test": "npm run build:happ && npm t -w tests",
    "start:happ": "hc s clean && RUST_LOG=warn hc s generate ./workdir/file-storage-test.happ --run=8888 -a test-app network mdns",
    "build:happ": "npm run build:zomes && hc app pack ./workdir/ --recursive",
    "build:zomes": "CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
