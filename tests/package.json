{
  "name": "tests",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "foreach -g \"**/*.test.ts\" -x \"TRYORAMA_LOG_LEVEL=warn NODE_NO_WARNINGS=1 ts-node-test #{path}\" --no-C"
  },
  "dependencies": {
    "@msgpack/msgpack": "^2.7.0",
    "@holochain/tryorama": "^0.11.0",
    "foreach-cli": "^1.8.1",
    "ts-node-test": "^0.2.0"
  },
  "type": "module"
}
