import { CoverageMapData, TestRunnerCoreConfig } from '@web/test-runner-core';
import { WebDriver } from 'selenium-webdriver';
/**
 * Manages tests to be excuted in iframes on a page.
 */
export declare class IFrameManager {
    private config;
    private driver;
    private framePerSession;
    private urlPerSession;
    private inactiveFrames;
    private frameCount;
    private initialized;
    private initializePromise?;
    private locked?;
    private isIE;
    constructor(config: TestRunnerCoreConfig, driver: WebDriver, isIE: boolean);
    private _initialize;
    isActive(id: string): boolean;
    private scheduleCommand;
    queueStartSession(id: string, url: string): Promise<void>;
    private startSession;
    queueStopSession(id: string): Promise<{
        testCoverage: CoverageMapData | undefined;
        errors: never[];
    }>;
    stopSession(id: string): Promise<{
        testCoverage: CoverageMapData | undefined;
        errors: never[];
    }>;
}
//# sourceMappingURL=IFrameManager.d.ts.map