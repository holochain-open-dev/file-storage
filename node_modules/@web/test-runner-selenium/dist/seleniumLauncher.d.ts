import { BrowserLauncher, TestRunnerCoreConfig } from '@web/test-runner-core';
import { Builder } from 'selenium-webdriver';
export interface SeleniumLauncherArgs {
    driverBuilder: Builder;
}
export declare class SeleniumLauncher implements BrowserLauncher {
    private driverBuilder;
    name: string;
    type: string;
    private config?;
    private driver?;
    private debugDriver;
    private iframeManager?;
    private __iframeManagerPromise?;
    private isIE;
    constructor(driverBuilder: Builder);
    initialize(config: TestRunnerCoreConfig): Promise<void>;
    stop(): Promise<void>;
    startSession(id: string, url: string): Promise<void>;
    isActive(id: string): boolean;
    stopSession(id: string): Promise<{
        testCoverage: import("istanbul-lib-coverage").CoverageMapData | undefined;
        errors: never[];
    }>;
    startDebugSession(_: string, url: string): Promise<void>;
    private ensureIframeManagerInitialized;
    private createiframeManager;
}
export declare function seleniumLauncher(args: SeleniumLauncherArgs): SeleniumLauncher;
//# sourceMappingURL=seleniumLauncher.d.ts.map