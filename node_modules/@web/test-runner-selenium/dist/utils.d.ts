import { Capabilities } from 'selenium-webdriver';
/**
 * Wraps a Promise with a timeout, rejecing the promise with the timeout.
 */
export declare function withTimeout<T>(promise: Promise<T>, message: string, timeout: number): Promise<T>;
export declare function getBrowserName(c: Capabilities): string;
export declare function getBrowserLabel(c: Capabilities): string;
//# sourceMappingURL=utils.d.ts.map