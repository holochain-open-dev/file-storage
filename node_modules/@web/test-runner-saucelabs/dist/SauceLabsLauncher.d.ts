import { TestRunnerCoreConfig } from '@web/test-runner-core';
import { SeleniumLauncher } from '@web/test-runner-selenium';
import { Capabilities } from 'selenium-webdriver';
import { SauceLabsLauncherManager } from './SauceLabsLauncherManager';
export declare class SauceLabsLauncher extends SeleniumLauncher {
    private manager;
    name: string;
    constructor(manager: SauceLabsLauncherManager, name: string, sauceLabsUrl: string, capabilities: Capabilities);
    startSession(sessionId: string, url: string): Promise<void>;
    startDebugSession(): Promise<void>;
    initialize(config: TestRunnerCoreConfig): Promise<void>;
    stop(): Promise<void>;
}
//# sourceMappingURL=SauceLabsLauncher.d.ts.map