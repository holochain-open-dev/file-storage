import * as tsModule from "typescript";
import { Node, TypeChecker } from "typescript";
interface Context {
    ts: typeof tsModule;
    checker?: TypeChecker;
    depth?: number;
    strict?: boolean;
}
/**
 * Takes a node and tries to resolve a constant value from it.
 * Returns undefined if no constant value can be resolved.
 * @param node
 * @param context
 */
declare function resolveNodeValue(node: Node | undefined, context: Context): {
    value: unknown;
    node: Node;
} | undefined;
export { Context, resolveNodeValue };
//# sourceMappingURL=chunk-resolve-node-value-db0a96cc.d.ts.map