import { Middleware } from 'koa';
import { Plugin } from '../Plugin';
export interface PluginServeMiddlewareConfig {
    plugins: Plugin[];
}
/**
 * Sets up a middleware which allows plugins to resolve the mime type.
 */
export declare function createPluginMimeTypeMiddleware(cfg: PluginServeMiddlewareConfig): Middleware;
